# Development / test dependencies.
# Phase 2 reuses httpx (already a runtime dep) for in-process ASGI testing
# via ``httpx.MockTransport`` / ``httpx.ASGITransport``, so no extra test
# transport library is required.

-r requirements.txt

pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
