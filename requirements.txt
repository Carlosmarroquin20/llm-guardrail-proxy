# Runtime dependencies.
# Pinned exactly to guarantee reproducible token counts, pricing arithmetic,
# and HTTP semantics across developer workstations and CI runners.

# Phase 1 — Tokenomics
tiktoken==0.8.0
pydantic==2.9.2

# Phase 2 — Async proxy surface
fastapi==0.115.4
uvicorn[standard]==0.32.0
httpx==0.27.2
pydantic-settings==2.6.1
